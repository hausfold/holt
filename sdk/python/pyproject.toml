[project]
name = "hausfold-holt"
version = "0.2.6"
description = "Python SDK for holt, the worktree-lifecycle substrate. Shells out to the holt binary; async-first so it drops into a FastAPI/asyncio backend or a script equally."
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.10"
authors = [{ name = "hausfold" }]
classifiers = [
  "Typing :: Typed",
]

[project.urls]
Repository = "https://github.com/hausfold/holt"

[project.optional-dependencies]
dev = ["pytest>=8", "pytest-asyncio>=0.24", "mypy>=1.11"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/holt"]

[tool.mypy]
strict = true
python_version = "3.10"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
